GCD.sh
2個以上の引数（整数）を与えられた場合、引数のGCD（Greatest Common Diviser：最大公約数）を返します。
与える引数の上限に論理的な制限はありません。引数が2個未満、整数ではない値を与えた場合、エラーを返して終了します。

Tester.sh
以下のテストシナリオを検証し、全てのテスト結果がPassだった場合、終了ステータス0でスクリプトを終了します。
 1.引数無しでスクリプトを実行。エラーで終了、エラーメッセージに"ARGERR"を含むことが期待。
 2.引数に文字列を与えてスクリプトを実行。エラーで終了、エラーメッセージに”TYPEERR”を含むことが期待。
 3&4.素数テーブルからランダムに選んだ2個の素数を掛けた値をGCDの期待値とする。素数テーブルからランダムに選んだ1～5個の素数を、決定したGCDに掛けて引数として与える数を生成する。Test3では2個、Test4では99個の引数を生成している。
   生成した引数を与えてスクリプトを実行し、スクリプトの戻り値が期待値と一致していれば、テストはPassである。
 5.素数テーブルからランダムで99個選んでスクリプトに引数として与える。全て素数なので、GCDの期待値は1である。

.travis.yml
Tester.shを呼び出してテストを実行する。終了ステータスが0の場合、全てのテストがPassしたと判定する。

prim_num.dat
Tester.shに与える素数テーブル。
